name: Log Input

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to log'
        required: true
        type: string

jobs:
  log-message:
    runs-on: ubuntu-latest
    steps:
      - name: Log input message
        run: echo "${{ github.event.inputs.message }}"
